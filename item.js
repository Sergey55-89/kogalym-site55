
import { db, doc, getDoc } from './firebase.js';
const lang=localStorage.getItem('kogalym_lang')||'ru';window.toggleLang=function(){localStorage.setItem('kogalym_lang',lang==='ru'?'en':'ru');location.reload();};window.toggleMenu=function(){document.getElementById('mobileMenu')?.classList.toggle('open')};function pick(o){return o?.[lang]||o?.ru||o?.en||''}
const p=new URLSearchParams(location.search),type=p.get('type')||'announcements',id=p.get('id'),box=document.getElementById('detailBox');
async function load(){if(!id){box.innerHTML='<div class="card"><p>Запись не найдена.</p></div>';return}try{const s=await getDoc(doc(db,type,id));if(!s.exists()){box.innerHTML='<div class="card"><p>Запись не найдена.</p></div>';return}const x=s.data(),title=pick(x.title),text=pick(x.text);box.innerHTML=`<article class="card detail-card">${x.imageUrl?`<img src="${x.imageUrl}" alt="">`:''}${x.category?`<span class="tag">${x.category}</span>`:''}<h1 style="color:#0b1b2b">${title}</h1><p style="font-size:20px;line-height:1.7">${text}</p>${x.phone?`<p><b>Телефон:</b> <a href="tel:${x.phone.replace(/\D/g,'')}">${x.phone}</a></p>`:''}<a class="btn ghost" href="javascript:history.back()">Назад</a></article>`}catch(e){box.innerHTML=`<div class="card"><p>Ошибка: ${e.message}</p></div>`}}load();
